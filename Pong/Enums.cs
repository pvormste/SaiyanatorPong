namespace Pong
{
    public enum ComponentType
    {
        Sprite,
        PlayerInput,
        BallBehavior,
        ComputerBehavior
    }

    public enum Input
    {
        Up,
        Down,
        None,
        Enter
    }

    public enum Entity
    {
        Player,
        Computer,
        Ball
    }
}